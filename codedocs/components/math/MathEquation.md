# MathEquation

Render a math expression using KaTeX.

## When to use

- Use to render math notation cleanly inside documentation.

## Props

- `block` - Optional prop.
- `className` - Additional CSS classes to apply.
- `expression` - Optional prop.

## Example

```jsx
<MathEquation expression="E = mc^2" block={false} />
```
