# Icon

Render a named icon from the shared icon set.

## When to use

- Use to render inline icons with a consistent design system.

## Props

- `className` - Additional CSS classes to apply.
- `name` - Optional prop.
- `size` - Optional prop.

## Example

```jsx
<Icon name="info" size={20} />
```
