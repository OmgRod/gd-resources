# ProgressRingChart

Render a circular progress indicator.

## Props

- `className` - Additional CSS classes to apply.
- `label` - Optional prop.
- `size` - Optional prop.
- `strokeWidth` - Optional prop.
- `value` - Optional prop.

## Example

```jsx
<ProgressRingChart value={75} />
```
